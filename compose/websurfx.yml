services:
  websurfx:
    image: websurfx:local
    container_name: websurfx
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - homelab

networks:
  homelab:
    external: true
