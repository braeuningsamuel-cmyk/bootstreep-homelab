services:
  websurfx:
    image: websurfx:local
    container_name: websurfx
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      - tor
    networks:
      - homelab

networks:
  homelab:
    external: true
