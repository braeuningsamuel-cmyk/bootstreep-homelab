---
# ChromaDB – Vektordatenbank für RAG (Retrieval Augmented Generation)
# Bootstreep Homelab v4.0

x-common: &common
  restart: unless-stopped
  security_opt: ["no-new-privileges:true"]
  cap_drop: [ALL]
  networks: [backend]

services:
  chromadb:
    <<: *common
    image: chromadb/chroma:0.5.5
    container_name: chromadb
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - chromadb_data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - PERSIST_DIRECTORY=/chroma/chroma
      - ANONYMIZED_TELEMETRY=FALSE
      # SECURITY: CHROMA_API_KEY should be set in .env (required for authentication)
      - CHROMA_SERVER_AUTH_CREDENTIALS=${CHROMA_API_KEY:?Error: CHROMA_API_KEY not set in .env}
      - CHROMA_SERVER_AUTH_CREDENTIALS_PROVIDER=chromadb.auth.token_authn.TokenAuthCredentialsProvider
    deploy:
      resources:
        limits: {memory: 2g, cpus: "2"}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 60s
      timeout: 5s
      retries: 5

volumes:
  chromadb_data:

networks:
  backend:
    external: true
