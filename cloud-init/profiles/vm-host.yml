#cloud-config
# vm-host.yml: Provisions a KVM virtualization host.

ansible:
  install_method: "pip"
  package_name: "ansible"
  pull:
    url: "https://github.com/braeuningsamuel-cmyk/bootstreep-homelab.git"
    playbook_name: "ansible/playbooks/vm-host.yml"
    checkout: "main"
